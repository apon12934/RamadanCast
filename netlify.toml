[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev -- --host --port 3000 --strictPort"
  targetPort = 3000

[[redirects]]
  from = "/api/tts"
  to = "/.netlify/functions/tts"
  status = 200
  force = true

